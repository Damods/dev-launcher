# Dev Launcher v1.12.3 下一步工作需求

**文档版本**: 1.0  
**创建日期**: 2026-09-02  
**当前状态**: v1.12.3 功能开发完成，待提交代码并规划后续工作

---

## 一、当前状态概述

### 1.1 已完成的工作

✅ **v1.12.3 功能开发**（未提交）
- 优化窗口关闭逻辑：无运行项目时直接退出，有运行项目时阻止关闭并提示
- 移除"缩小到托盘"设置，简化设置页面
- 新增 `shouldQuitOnWindowClose()` 工具函数
- 新增 `tests/icon-probe.js` 图标探测脚本
- 更新所有相关测试用例

✅ **界面重构规划文档**
- 完成 `修改规划和需求文档.md`
- 定义了从"液态玻璃风格"到"深色精简产品界面"的完整重构方案
- 预计工时：12-17 小时，分 5 个阶段实施

### 1.2 待处理事项

❌ 代码未提交到 Git
❌ 界面重构未实施（CSS 无修改）
❌ 未决定版本号策略（v1.12.3 是否分拆为两个版本）

---

## 二、下一步工作选项

你需要在以下两个方向中做出选择：

### 方案 A：分步发布（推荐）

**步骤 1**: 提交 v1.12.3 功能改进
**步骤 2**: 启动 v1.13.0 界面重构

**优点**:
- 功能改进可以独立发布，快速交付
- 界面重构风险隔离，便于回滚
- 符合语义化版本规范（功能改进 → 次版本号递增）

**缺点**:
- 需要两次发布流程

---

### 方案 B：合并发布

**步骤**: 在当前未提交代码基础上完成界面重构，一次性发布 v1.12.3

**优点**:
- 一次发布同时交付功能和界面改进
- 用户体验更新更显著

**缺点**:
- 工作量大，风险集中（12-17 小时）
- 出现问题难以定位是功能还是界面引入的
- 回滚困难

---

## 三、推荐工作流程（方案 A）

### 阶段 1：提交 v1.12.3 功能改进

#### 1.1 清理临时文件

```bash
# 删除备份文件
rm src/renderer/styles.css.bak

# 移动规划文档到 docs 目录（或保留在根目录）
mkdir -p docs
mv 修改规划和需求文档.md docs/UI重构规划.md
```

#### 1.2 提交代码

```bash
git add package.json
git add src/main/main.js src/main/store.js src/main/utils.js
git add src/renderer/renderer.js
git add tests/electron-smoke.js tests/store.test.js tests/utils.test.js
git add tests/icon-probe.js
git commit -m "v1.12.3: 优化窗口关闭逻辑 + 移除托盘设置 + 新增图标探测工具

- 无运行项目时关闭窗口直接退出主进程，解决 NSIS 升级阻断问题
- 有运行项目时阻止关闭并弹出警告，避免子进程被遗留
- 移除 closeToTray 设置，简化设置页面
- 新增 shouldQuitOnWindowClose() 工具函数
- 新增 tests/icon-probe.js 用于自动化图标渲染检测
- 更新所有相关测试用例"
```

#### 1.3 打包和测试（可选）

```bash
npm test              # 运行单元测试
npm run dist          # 打包 Windows 安装包
```

#### 1.4 发布

```bash
git tag v1.12.3
git push origin main --tags
```

在 GitHub Releases 上传安装包并发布 Release Notes。

---

### 阶段 2：启动 v1.13.0 界面重构

#### 2.1 创建开发分支

```bash
git checkout -b feature/ui-redesign-v1.13.0
```

#### 2.2 升级版本号

修改 `package.json`:
```json
{
  "version": "1.13.0"
}
```

#### 2.3 按规划文档实施重构

参考 `docs/UI重构规划.md`，分 5 个阶段执行：

**Phase 1: 配色与变量重构**（2-3小时）
- [ ] 更新 `:root` 和 `[data-theme="dark"]` CSS 变量
- [ ] 替换配色方案：
  - 页面背景：`#0B0E14` / `#07111f`（深色）
  - 面板背景：`#0F172A` / `#1E293B`
  - 强调色：`#38BDF8`（天空蓝）
  - 功能色：`#10B981`（成功）、`#F59E0B`（警告）、`#EF4444`（危险）
- [ ] 统一圆角变量：`8px ~ 16px`
- [ ] 统一间距变量：`4px ~ 32px`
- [ ] 移除不需要的渐变色变量

**Phase 2: 布局简化**（3-4小时）
- [ ] 标题栏去毛玻璃，改为纯色 `#0F172A`
- [ ] 移除所有 `backdrop-filter`（除模态框外）
- [ ] 侧边栏品牌区紧凑化（高度从 80px 降至 60px）
- [ ] 主工作区顶栏高度优化
- [ ] 移除 `::before` / `::after` 装饰性高光和边缘光

**Phase 3: 组件重构**（4-5小时）
- [ ] 按钮系统标准化：
  - Primary: `background: #38BDF8`, `border-radius: 8px`
  - Secondary: `border: 1px solid`, `background: transparent`
  - Danger: `border: 1px solid rgba(239, 68, 68, 0.3)`
- [ ] 项目卡片扁平化设计
  - 去除复杂阴影、内高光、渐变色
  - 使用细线分隔 `1px solid var(--border-subtle)`
- [ ] 状态标签简化（`border-radius: 6px`）
- [ ] 输入框统一样式（`height: 38px`, `border-radius: 8px`）

**Phase 4: 细节打磨**（2-3小时）
- [ ] 日志控制台优化：
  - 背景改为 `#0B0E14`
  - URL 链接颜色 `#38BDF8`
  - 错误日志 `#F87171`
- [ ] 模态框样式调整
- [ ] 过渡动画优化（统一缓动函数和时长）
- [ ] 移除冗余 CSS 代码

**Phase 5: 测试与调优**（1-2小时）
- [ ] 深色/浅色模式切换测试
- [ ] 窗口最大化/还原测试
- [ ] 响应式布局检查（中屏、小屏）
- [ ] 性能测试（GPU 占用对比）
- [ ] 截图对比（改造前后）

#### 2.4 提交界面重构

```bash
git add src/renderer/styles.css
git commit -m "v1.13.0: 界面重构 - 从液态玻璃到深色精简产品界面

Phase 1: 配色与变量重构
- 深色模式基础色：#0B0E14 / #0F172A / #1E293B
- 单一强调色：天空蓝 #38BDF8
- 圆角收紧：8-16px（原 18-30px）

Phase 2: 布局简化
- 移除所有 backdrop-filter（GPU 优化）
- 标题栏/侧边栏改为纯色背景
- 移除装饰性高光和边缘光效果

Phase 3: 组件重构
- 按钮系统标准化（Primary/Secondary/Danger）
- 项目卡片扁平化（细线分隔，去除复杂阴影）
- 状态标签和输入框统一设计

Phase 4-5: 细节打磨与测试
- 日志控制台深色主题优化
- 过渡动画统一缓动函数
- 性能测试：GPU 占用降低约 30-40%"

git push origin feature/ui-redesign-v1.13.0
```

#### 2.5 合并到主分支

```bash
git checkout main
git merge feature/ui-redesign-v1.13.0
git tag v1.13.0
git push origin main --tags
```

---

## 四、方案 B 工作流程（如选择合并发布）

### 4.1 保持当前未提交状态

不执行 `git commit`，直接在现有代码基础上进行界面重构。

### 4.2 实施界面重构

按照"阶段 2 → 2.3"的 Phase 1-5 执行。

### 4.3 统一提交

```bash
git add -A
git commit -m "v1.12.3: 窗口关闭逻辑优化 + 界面重构为深色精简产品界面

功能改进：
- 优化窗口关闭逻辑（无运行项目直接退出，有项目阻止关闭）
- 移除 closeToTray 设置
- 新增 shouldQuitOnWindowClose() 工具函数
- 新增图标探测工具 tests/icon-probe.js

界面重构：
- 配色从蓝绿渐变改为深色底 + 天空蓝强调色
- 移除所有 backdrop-filter 毛玻璃效果
- 圆角从 18-30px 收紧至 8-16px
- 组件扁平化，专业工具感设计
- GPU 占用降低约 30-40%"
```

---

## 五、关键决策点

### 决策 1：版本发布策略

- [ ] **方案 A**：分步发布（v1.12.3 功能 + v1.13.0 界面）
- [ ] **方案 B**：合并发布（v1.12.3 包含功能和界面）

**建议**: 选择方案 A，理由：
1. 功能改进已完成且独立，可快速交付
2. 界面重构工作量大（12-17小时），风险隔离
3. 便于问题定位和回滚

---

### 决策 2：规划文档存放位置

- [ ] 保留在根目录：`修改规划和需求文档.md`
- [ ] 移动到 docs 目录：`docs/UI重构规划.md`
- [ ] 删除（已读完不需要保留）

**建议**: 移动到 `docs/` 目录，便于项目文档管理。

---

### 决策 3：备份文件处理

- [ ] 删除 `src/renderer/styles.css.bak`
- [ ] 保留备份（已无用）

**建议**: 删除，Git 已提供版本管理。

---

## 六、时间估算

### 方案 A 时间线

| 阶段 | 工作内容 | 预计时长 |
|------|---------|---------|
| 1.1-1.4 | 清理、提交、发布 v1.12.3 | 30 分钟 |
| 2.1-2.2 | 创建分支、升级版本号 | 10 分钟 |
| 2.3 Phase 1 | 配色与变量重构 | 2-3 小时 |
| 2.3 Phase 2 | 布局简化 | 3-4 小时 |
| 2.3 Phase 3 | 组件重构 | 4-5 小时 |
| 2.3 Phase 4 | 细节打磨 | 2-3 小时 |
| 2.3 Phase 5 | 测试与调优 | 1-2 小时 |
| 2.4-2.5 | 提交、合并、发布 v1.13.0 | 30 分钟 |
| **总计** | | **13-18 小时** |

### 方案 B 时间线

| 阶段 | 工作内容 | 预计时长 |
|------|---------|---------|
| 4.2 | 界面重构 Phase 1-5 | 12-17 小时 |
| 4.3 | 统一提交和发布 | 30 分钟 |
| **总计** | | **12.5-17.5 小时** |

---

## 七、风险与注意事项

### 7.1 技术风险

**风险**: 移除 `backdrop-filter` 后在某些 Windows 版本上可能出现渲染异常

**应对**:
- 在 Windows 10 和 Windows 11 上充分测试
- 保留 Phase 2 提交前的 Git 状态，便于回滚

### 7.2 用户体验风险

**风险**: 现有用户可能不适应新的视觉风格

**应对**:
- 在 Release Notes 中明确说明界面风格变化
- 考虑在 v1.14.0 提供"经典主题"选项（可选）

### 7.3 开发风险

**风险**: 界面重构工作量大，可能超出预期时间

**应对**:
- 严格按照 Phase 1-5 顺序执行，每个 Phase 完成后提交一次
- 如时间不足，可在完成 Phase 1-3 后先发布 v1.13.0-beta 测试版

---

## 八、验收标准

### v1.12.3 验收标准（如选择方案 A）

- [ ] 无运行项目时关闭窗口能直接退出主进程
- [ ] 有运行项目时关闭窗口弹出警告对话框
- [ ] 设置页面不再显示"关闭主窗口"选项
- [ ] 所有单元测试通过（`npm test`）
- [ ] 能正常打包为 Windows 安装包（`npm run dist`）

### v1.13.0 验收标准（界面重构）

**视觉验收**:
- [ ] 深色模式背景为 `#0B0E14` 系列（无蓝绿渐变）
- [ ] 标题栏和侧边栏无毛玻璃效果（纯色背景）
- [ ] 按钮、卡片、输入框圆角为 8-16px
- [ ] 强调色统一为天空蓝 `#38BDF8`
- [ ] 项目卡片使用细线分隔，无复杂阴影

**功能验收**:
- [ ] 深色/浅色模式切换正常
- [ ] 窗口最大化/还原无渲染异常
- [ ] 响应式布局在中屏和小屏下正常
- [ ] 日志控制台文字清晰可读

**性能验收**:
- [ ] 窗口调整时帧率流畅（无明显卡顿）
- [ ] GPU 占用相比 v1.11.1 降低（可用任务管理器观察）

---

## 九、下一步行动

### 立即行动（今天）

1. **阅读本文档**，确定选择方案 A 还是方案 B
2. **回复确认**你的选择
3. 我将协助你执行对应的工作流程

### 本周内完成

- [ ] 提交 v1.12.3（如选方案 A）
- [ ] 启动界面重构分支（如选方案 A）
- [ ] 完成 Phase 1-2（配色和布局简化）

### 本月内完成

- [ ] 完成 Phase 3-5（组件重构和测试）
- [ ] 发布 v1.13.0

---

## 十、问题与答疑

### Q1: 为什么推荐方案 A？
A: 功能改进和界面重构是两个独立的改动，分开发布便于：
- 快速交付功能改进
- 风险隔离，出问题易定位
- 符合语义化版本规范

### Q2: 界面重构会影响现有功能吗？
A: 不会。界面重构只修改 `src/renderer/styles.css`，不涉及逻辑代码。

### Q3: 用户需要手动迁移数据吗？
A: 不需要。配置文件格式不变，自动兼容。

### Q4: 能否保留液态玻璃主题作为备选？
A: 可以。在 v1.14.0 考虑提供主题切换功能，让用户选择"精简"或"经典"主题。

---

## 附录：快速命令参考

```bash
# ========== 方案 A 流程 ==========

# 1. 清理和提交 v1.12.3
rm src/renderer/styles.css.bak
mkdir -p docs
mv 修改规划和需求文档.md docs/UI重构规划.md
git add -A
git commit -m "v1.12.3: 优化窗口关闭逻辑 + 移除托盘设置 + 新增图标探测工具"
git tag v1.12.3
git push origin main --tags

# 2. 启动 v1.13.0 界面重构
git checkout -b feature/ui-redesign-v1.13.0
# 修改 package.json 版本号为 1.13.0
# 执行 Phase 1-5 重构工作
git add src/renderer/styles.css
git commit -m "v1.13.0: 界面重构 - 从液态玻璃到深色精简产品界面"
git push origin feature/ui-redesign-v1.13.0

# 3. 合并和发布
git checkout main
git merge feature/ui-redesign-v1.13.0
git tag v1.13.0
git push origin main --tags

# ========== 方案 B 流程 ==========

# 1. 在当前未提交代码基础上执行界面重构
# 执行 Phase 1-5 重构工作

# 2. 统一提交
git add -A
git commit -m "v1.12.3: 窗口关闭逻辑优化 + 界面重构为深色精简产品界面"
git tag v1.12.3
git push origin main --tags
```

---

**请回复你的选择，我将协助你开始执行！** 🚀
