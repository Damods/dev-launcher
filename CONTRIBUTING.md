# 参与贡献

感谢你对 Dev Launcher 的关注！

## 报告问题

提交前请先搜索是否已有相同或类似的问题。

- **Bug 报告**：请使用 Bug report 模板，包含复现步骤、期望结果、实际结果、系统版本与日志片段。
- **功能建议**：请使用 Feature request 模板，说明使用场景与期望行为。

## 提交 Pull Request

1. Fork 本仓库并从 `main` 分支创建你的特性分支。
2. 提交前请确保 `npm test` 全部通过。
3. 保持改动聚焦，避免引入与本次变更无关的修改。
4. 在 PR 描述中清晰说明改动内容与原因。

## 本地开发

```powershell
npm install
npm start      # 启动桌面应用
npm test       # 运行单元测试
npm run dist   # 打包 Windows NSIS 安装包
```

## 行为准则

请保持友善与尊重，共同维护健康的开源社区。
